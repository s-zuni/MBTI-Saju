{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/((?!static|favicon.ico|logo.*|manifest.json|robots.txt).*)",
            "destination": "/index.html"
        }
    ]
}